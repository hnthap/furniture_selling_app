{
  "name": "furniture_app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm install && cd backend && npm install && npm run build && cd ../frontend && npm install && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
